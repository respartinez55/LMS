specVersion: "v1beta"
serviceId: "lms"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "lms-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
